query TextSearch($search: String!, $first: Int) {
  domains(
    first: $first
    orderBy: name
    orderDirection: asc
    where: { name_contains_nocase: $search }
  ) {
    id
    name
    labelName
    owner {
      id
    }
  }
}
